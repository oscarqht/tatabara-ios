import SwiftUI

struct HistoryPlaceholderView: View {
    var body: some View {
        PlaceholderScreen(
            eyebrow: "Telemetry",
            title: "History",
            message: "Completed sessions and streaks will land here after the MVP workout flow is locked in."
        )
    }
}
