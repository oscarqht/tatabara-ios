import SwiftUI

struct ProfilePlaceholderView: View {
    var body: some View {
        PlaceholderScreen(
            eyebrow: "Identity",
            title: "Profile",
            message: "Future versions can add preferences, cue voice selection, and workout personalization from this space."
        )
    }
}
